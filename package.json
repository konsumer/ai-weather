{
  "name": "weather",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "weather:build": "ollama create konsumer/weather -f weather.Modelfile",
    "weather": "node server.js",
    "test": "DEBUG=true npm run weather what kind of weather can I expect today, in Portland, OR?"
  },
  "dependencies": {
    "@tinoschroeter/weather-js": "^3.0.2",
    "ollama": "^0.4.9"
  }
}
